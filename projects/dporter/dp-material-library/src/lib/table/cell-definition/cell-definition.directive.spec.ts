import { CellDefinitionDirective } from './cell-definition.directive';

describe('CellDefinitionDirective', () => {
  it('should create an instance', () => {
    const directive = new CellDefinitionDirective();
    expect(directive).toBeTruthy();
  });
});
